# PeerHelp Django Application

## Getting Started

1. Install dependencies: `pip install -r requirements.txt`.
2. Apply migrations and create a superuser if needed.
3. From the `projectsite/` directory run `python manage.py runserver`.
4. Visit the site to post problems, browse requests, and use the in-person map view.
